@model IEnumerable<BooksForAdoption.Models.Transactions>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RequestID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RequestID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TransactionsNumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TransactionsNumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TransactionsNumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
